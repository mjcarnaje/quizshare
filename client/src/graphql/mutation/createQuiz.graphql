mutation CreateQuiz(
	$title: String!
	$description: String!
	$quiz_photo: String
	$questions: [QuestionInput!]!
	$results: [ResultInput!]
	$tags: [TagInput!]
) {
	createQuiz(
		data: {
			title: $title
			description: $description
			quiz_photo: $quiz_photo
			questions: $questions
			results: $results
			tags: $tags
		}
	) {
		title
		description
		quiz_photo
		questions {
			question_id
			question
			question_photo
			choices
			answer
			explanation
			with_explanation
			hint
			with_hint
		}
		results
		tags {
			name
		}
	}
}
