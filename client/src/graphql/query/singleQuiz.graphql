query SingleQuiz($quiz_id: Int!) {
	singleQuiz(quiz_id: $quiz_id) {
		id
		quiz_photo
		title
		description
		author {
			id
			username
			avatar
			email
			profile {
				name
			}
		}
		is_liked
		likes_count
		comments_count
		created_at
		questionsCount
		takers_count
		is_taken
	}
}
