query QuizToUpdate($quiz_id: Int!) {
	quiz_to_update(quiz_id: $quiz_id) {
		title
		description
		quiz_photo
		questions {
			question_id
			question
			question_photo
			choices
			answer
			explanation
			with_explanation
			hint
			with_hint
		}
		results
		tags {
			name
		}
	}
}
