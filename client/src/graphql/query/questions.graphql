query Questions($quiz_id: Int!, $withAnswer: Boolean!) {
	questions(quiz_id: $quiz_id) {
		question_id
		question
		question_photo
		choices
		hint
		with_hint
		answer @include(if: $withAnswer)
	}
}
