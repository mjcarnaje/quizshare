query SearchedQuizzes(
	$limit: Int!
	$query: String!
	$cursor: String
	$sort_by: String
) {
	searched_quizzes(
		limit: $limit
		query: $query
		cursor: $cursor
		sort_by: $sort_by
	) {
		has_more
		quizzes {
			...QuizzesResponse
		}
	}
}
