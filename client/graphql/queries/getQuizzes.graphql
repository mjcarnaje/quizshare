query GetQuizzes(
  $input: GetQuizzesInput!
  $isPublished: Boolean!
  $isMine: Boolean!
) {
  getQuizzes(input: $input, isPublished: $isPublished, isMine: $isMine) {
    quizzes {
      ...QuizCard
    }
    pageInfo {
      ...PageInfo
    }
  }
}
