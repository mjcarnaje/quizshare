mutation SignIn(
  $usernameOrEmail: String!
  $password: String!
  $rememberMe: Boolean!
) {
  signIn(
    SignInInput: {
      usernameOrEmail: $usernameOrEmail
      password: $password
      rememberMe: $rememberMe
    }
  ) {
    ...userResponse
  }
}
